apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-dashboard-parent.fullname" . }}
  labels:
    {{- include "kubernetes-dashboard-parent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-dashboard-parent.selectorLabels" . | nindent 4 }}
