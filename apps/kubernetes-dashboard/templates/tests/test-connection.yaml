apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubernetes-dashboard-parent.fullname" . }}-test-connection"
  labels:
    {{- include "kubernetes-dashboard-parent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubernetes-dashboard-parent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
