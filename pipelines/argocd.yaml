apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-argo-cd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Umershamshad53/argocd.git
    targetRevision: HEAD
    path: apps/argo-cd
    helm:
      releaseName: my-argo-cd          # optional: if you have custom values files
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
